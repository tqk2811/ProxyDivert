<UserControl x:Class="ProxyDivert.Wpf.Views.ConnectionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource Card}" Margin="4">
        <DockPanel>
            <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                <TextBlock Style="{StaticResource Heading}" Text="{DynamicResource Str.Tab.Connections}"
                           VerticalAlignment="Center"/>
                <TextBlock Style="{StaticResource Subtle}" VerticalAlignment="Center" Margin="10,0,0,0"
                           Text="{Binding ActiveCount, StringFormat={}({0})}"/>
                <TextBox Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged, Delay=250}"
                         Width="240" Margin="16,0,0,0" HorizontalAlignment="Left"/>
                <CheckBox Content="{DynamicResource Str.Conn.ShowClosed}" IsChecked="{Binding ShowClosed}"
                          VerticalAlignment="Center" Margin="16,0,0,0"/>
                <Button Content="{DynamicResource Str.App.Clear}" Command="{Binding ClearHistoryCommand}"
                        HorizontalAlignment="Right" MinWidth="80"/>
            </DockPanel>

            <DataGrid ItemsSource="{Binding Connections}" AutoGenerateColumns="False"
                      CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Str.Process.Pid}" Binding="{Binding ProcessId}" Width="70"/>
                    <DataGridTextColumn Header="{DynamicResource Str.Conn.Process}" Binding="{Binding ProcessName}" Width="140"/>
                    <DataGridTextColumn Header="{DynamicResource Str.Conn.Host}" Binding="{Binding Host}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource Str.Conn.Destination}" Binding="{Binding Destination}" Width="160"/>
                    <DataGridTextColumn Header="{DynamicResource Str.Conn.Outbound}" Binding="{Binding OutboundName}" Width="120"/>
                    <DataGridTextColumn Header="{DynamicResource Str.Conn.Reason}" Binding="{Binding RouteReason}" Width="180"/>
                    <DataGridTextColumn Header="{DynamicResource Str.Conn.Up}"
                                        Binding="{Binding BytesUp, Converter={StaticResource ByteSize}}" Width="80"/>
                    <DataGridTextColumn Header="{DynamicResource Str.Conn.Down}"
                                        Binding="{Binding BytesDown, Converter={StaticResource ByteSize}}" Width="80"/>
                    <DataGridTextColumn Header="{DynamicResource Str.Conn.Duration}"
                                        Binding="{Binding StartedUtc, Converter={StaticResource Duration}}" Width="80"/>
                </DataGrid.Columns>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                        <Style.Triggers>
                            <!-- A finished connection reads as history; a failed one has to stand out. -->
                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Error, Converter={StaticResource IsNullOrEmpty}}" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Brush.Danger}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </DockPanel>
    </Border>
</UserControl>
